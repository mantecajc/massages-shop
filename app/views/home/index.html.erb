<div class="flex justify-between items-center">
  <%= link_to "Edit", edit_home_path(@home.id), data: { turbo_frame: "modal" }, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
</div>
<%= render 'home_body', home: @home %>
