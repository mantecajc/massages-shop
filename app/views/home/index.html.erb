<% if user_signed_in? %>
  <div class="hidden md:flex h-[60px] justify-end px-8">
    <div class="flex items-center">
      <%= link_to "Editer", edit_home_path(@home.id), data: { turbo_frame: "modal" }, class: "text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center" %>
    </div>
  </div>
<% end %>

<%= render 'home_body', home: @home %>
